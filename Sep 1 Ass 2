#include <stdio.h>
int fact(int);
int main()
{
    int n,x;
    printf("\nEnter the value of n");
    scanf("%d",&n);
    x=fact(n);
    printf("The factorial is %d",x);
}
int fact(int x)
{
    int i=1, f=1;
    while(i<=x)
    {
        f=f*i;
        i++;
    }
    return f;
}

    

